<h1>Thank you for liking this pin!</h1>

<p id="notice"><%= notice %></p>
<div id = "pinshow">
<table>
	<tbody>
		<th><%= @pin.name %></th>
		<tr></tr>
		<td><%= image_tag(@pin.image_url, :width => 200) if @pin.image.present? %>

		</div>
	</tbody>
</table>
</div>
<p>
	From <em><%= @pin.board.name rescue "N/A"%></em> 
	<br>
	by <em><%= @pin.user.first_name %></em>
</p>



<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>